---
- name: Provision Ubuntu 26.04 node
  hosts: k3s_cluster
  become: yes

  tasks:

    - name: Update apt cache
      apt:
        update_cache: yes

    - name: Upgrade system packages
      apt:
        upgrade: dist
        autoremove: yes

    - name: Install base packages
      apt:
        name:
          - curl
          - wget
          - git
          - jq
          - nfs-common
          - net-tools
          - htop
          - unzip
          - ca-certificates
          - open-iscsi
          - wireguard-tools
        state: present

    - name: Install Intel GPU drivers
      apt:
        name:
          - intel-media-va-driver-non-free
          - i965-va-driver-shaders
          - vainfo
        state: present

    - name: Ensure /dev/dri exists
      stat:
        path: /dev/dri
      register: dri_path

    - name: Warn if GPU not detected
      debug:
        msg: "WARNING: /dev/dri not found — hardware acceleration may not work"
      when: not dri_path.stat.exists

    - name: Enable kernel modules for container workloads
      copy:
        dest: /etc/modules-load.d/k8s.conf
        content: |
          br_netfilter
          overlay
          wireguard
          i915

    - name: Disable Swap (Required for k3s)
      command: swapoff -a
      when: ansible_memory_mb.swap.total | default(0) > 0

    - name: Permanently Disable Swap in /etc/fstab
      replace:
        path: /etc/fstab
        regexp: '^([^#].*swap.*)$'
        replace: '# \1'

    - name: Enable iSCSI Service
      systemd:
        name: iscsid
        enabled: yes
        state: started

    - name: Add user to 'render' group
      user:
        name: "{{ ansible_user }}"
        groups: render
        append: yes

    - name: Apply sysctl settings
      copy:
        dest: /etc/sysctl.d/99-k8s.conf
        content: |
          net.ipv4.ip_forward = 1
          net.bridge.bridge-nf-call-iptables = 1
          net.bridge.bridge-nf-call-ip6tables = 1

    - name: Reload sysctl
      command: sysctl --system

    - name: Reboot if required
      reboot:
        msg: "Rebooting after provisioning"
        connect_timeout: 5
        reboot_timeout: 600

